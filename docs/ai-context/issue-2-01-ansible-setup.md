# Issue #2: Ansible Proxmox VM 자동화 설계 및 초기화

## 📅 날짜: 2025-12-29
## 👤 참여자: 임성현, Gemini

## 💬 대화 요약
- **인프라 제약 조건 확인**: 
  - Proxmox 노드 6대(101~106), NFS 공유 스토리지 사용.
  - 901(Ubuntu 24.04), 902(K8s-base) 템플릿 활용.
- **프로비저닝 로직 합의**: 
  - 템플릿을 타겟 노드로 `migrate` 후 `clone`, 완료 후 다시 `server-01`로 원복하는 순차 처리 방식.
  - API Polling을 통해 작업 완료 상태를 확인한 뒤 후속 작업(원복, K8s 설정 등) 진행.
- **플레이북 구조화**: 
  - 일반 Ubuntu VM 생성과 K8s 클러스터 구축 로직을 분리하여 관리하기로 함.

## 🛠️ 결정된 사항
1. **변수 관리**: `group_vars/all.yml`에 API Token, SSH Key 경로 등 전역 설정 배치.
2. **K8s 자동화**: 마스터의 조인 커맨드를 동적으로 추출하여 워커 노드에 전달하는 `fact sharing` 기법 적용.
3. **보안**: 모든 Ansible 명령은 `vm-bastion`(Tailscale VPN)에서 수행하며 SSH Key 주입 자동화.

## 🔗 다음 작업
- [ ] `vars/vm_list.yml` 정의 (12대 VM 리스트 확정)
- [ ] `provision_ubuntu.yml` (901 기반) 플레이북 작성
